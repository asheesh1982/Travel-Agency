{
  "name": "car-rental-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute car-rental-db --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute car-rental-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.5.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
